# Phụ thuộc của control plane (dataplatform/) — chỉ dùng lúc phát triển,
# không đi vào runtime của bất kỳ service nào trong docker-compose.
#
#   pip install -r requirements-dev.txt
#   python -m dataplatform.cli check

PyYAML>=6.0          # đọc dataset contract
jsonschema>=4.0      # validate contract theo dataplatform/schemas/*.json
sqlglot>=25.0        # parse SQL batch Spark -> lineage cột (generators/lineage.py, ADR-0028)
